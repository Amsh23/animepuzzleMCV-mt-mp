# 🧩 Anime Puzzle MCV-MT-MP

یک بازی پازل تعاملی با معماری **MVC** و پشتیبانی **Multi-Threading و Multi-Processing** برای حداکثر کارایی.

## 📋 درباره پروژه

این پروژه یک بازی پازل عکس است که تصاویر را به تیکه‌های مختلف تقسیم می‌کند و بازیکن باید آن‌ها را در زمان مشخص مرتب کند. 
استفاده از معماری **MVC** و فناوری‌های **Multi-Threading/Processing** باعث عملکرد بهتر و کد سازمان‌یافته‌تر می‌شود.

## ✨ ویژگی‌ها

- 🎨 **پشتیبانی تصاویر متعدد** - استفاده از تصاویر PNG/JPG
- 🧵 **Multi-Threading/Processing** - عملکرد بهتر در پردازش تصاویر
- 🎮 **رابط گرافیکی** - Tkinter برای تجربه کاربری بهتر
- ⏱️ **سیستم تایمر** - ردیابی زمان بازی
- 📊 **سیستم امتیازات** - تعداد حرکات و زمان بازی
- 🌐 **ارسال آنلاین** - ذخیره نتایج به صورت آنلاین
- 🔤 **پشتیبانی فارسی** - رابط کاملاً فارسی‌زبان

## 🏗️ ساختار پروژه

```
animepuzzleMCV-mt-mp/
├── withMCV/              # پوشه اصلی پروژه MVC
│   ├── main.py          # نقطه شروع برنامه
│   ├── model.py         # مدل (Model) - منطق بازی
│   ├── view.py          # نمای (View) - رابط گرافیکی
│   └── controller.py    # کنترلر (Controller) - هماهنگی
├── multithreading&multiprocess/
│   └── test_threads_processes.py  # تست Multi-Threading/Processing
├── animepuzzle.py       # فایل اصلی PyInstaller
├── animepuzzle.spec     # تنظیمات PyInstaller
└── README.md
```

## 🚀 نحوه استفاده

### نیازمندی‌ها
```bash
pip install pillow
```

### اجرای برنامه
```bash
cd withMCV
python main.py
```

برنامه از شما نام بازیکن را می‌پرسد، سپس پازل را شروع می‌کند.

## 🎯 چگونه بازی کنیم

1. **نام خود را وارد کنید** - پنجره پاپ‌آپ ظاهر می‌شود
2. **تیکه‌های پازل را مرتب کنید** - تصویر را کامل کنید
3. **زمان و امتیاز را ببینید** - سیستم تایمر کار می‌کند
4. **ارسال آنلاین** - دکمه "ارسال آنلاین" را فشار دهید

## 📁 توضیح فایل‌ها

### `model.py`
مدل اصلی که شامل:
- بارگذاری و بریدن تصویر
- مدیریت موقعیت‌های پازل
- ردیابی تعداد حرکات

### `view.py`
رابط گرافیکی که شامل:
- نمایش تیکه‌های پازل روی Canvas
- نمایش تایمر
- مدیریت UI

### `controller.py`
کنترلر که شامل:
- کنترل تعاملات بازیکن
- هماهنگی بین Model و View
- مدیریت منطق بازی

### `main.py`
نقطه شروع برنامه:
- ایجاد نافذه Tkinter
- مقداردهی اولیه MVC
- شروع حلقه اصلی

## ⚙️ تنظیمات

در `main.py` می‌توانید این مقادیر را تغییر دهید:
```python
WINDOW_SIZE = 600      # اندازه پنجره (پیکسل)
GRID_SIZE = 3          # تعداد سطرها و ستون‌ها (3x3 = 9 تیکه)
IMAGE_FILE = "bleach.jpg"  # نام فایل تصویر
```

## 🔧 ساخت فایل اجرایی

```bash
pyinstaller animepuzzle.spec
```

فایل اجرایی در پوشه `dist/` ایجاد خواهد شد.

## 📌 پروژه‌های مرتبط

اگر به پروژه‌های مشابه علاقه‌مند هستید، [Python MVC Forum](https://github.com/Amsh23/Python-MVC-Forum) را ببینید - یک پلتفرم انجمن با معماری MVC کامل.
